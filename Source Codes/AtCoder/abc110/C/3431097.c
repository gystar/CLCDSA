#include<stdio.h>
#include<string.h>

#define MYSTRINGMAX 200000 // ??20????
#define SIZE MYSTRINGMAX + 2 // MYSTRINGMAX+2????SIZE??????????? SIZE = 200002

int main(void) {
	char s[SIZE], t[SIZE], c1, c2;

	fgets(s, SIZE, stdin);
	fgets(t, SIZE, stdin);

	int length = strlen(s); // length??????????strlen?????s??????
	
	for (int i = 0; i < length; i++) {
		if (s[i] != t[i]) { // s[i]?t[j]?????????????
			c1 = s[i]; // c1?if??????????s???
			c2 = t[i]; // c2?if??????????t???
			for (int j = 0; j < i; j++) { // ???????????????????"No"
				if (s[j] == c2 || s[j] == c1) {
					printf("No\n");
					return 0;
				}
			}
			for (int j = i; j < length; j++) {
				if (s[j] == c1) {
					s[j] = c2; // s[j]????????c2???????
				}
				else if (s[j] == c2) {
					s[j] = c1; 
				}
			}
		}
	}
	printf("Yes\n"); // ??????Yes???
	return 0;
} 
//?????????//
#include <stdio.h>
int max(int a,int b)
{
  int max = a;
  if (b > a)	max =b;
  return max;
}
int main(void)
{
  //??//
  int n;
  scanf("%d",&n);
  int l[n];
  int i;
  for (i =0; i < n; i++ )	{
    scanf("%d",&l[i]);
  }
  //??//
  //????????????//
  int maxi = 0;
  int place = 0;
  for(i =0; i< n; i++)	{
    maxi = max(maxi,l[i]);
    if(maxi == l[i]) place =i; 
  }
  //????maxi?????,place?????????????????????//
  int sum = 0;
  for(i = 0; i <n; i++)	{
    sum = sum + l[i];
  }
  sum = sum - l[place];
  //???sum???????????????//
  if(sum > maxi)	printf("Yes");
  else printf("No");
  return 0;
} 
#include <stdio.h>

int main(int argc, char const *argv[]) {
    // ??
    int N;
    // ??
    scanf("%d", &N );
    // ??
    int ans;
    ans = (N%2==0 ? N/2:N/2+1);
    // ??
    printf("%d\n", ans );
    return 0;
} 
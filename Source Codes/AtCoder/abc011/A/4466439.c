#include <stdio.h>

int main(int argc, char const *argv[]) {
    // ??
    int N, ans;
    // ??
    scanf("%d", &N );
    // ??
    if (N==12) {
        ans =1;
        printf("%d\n", ans );
    } else {
        printf("%d\n", N+1 );
    }
    return 0;
} 
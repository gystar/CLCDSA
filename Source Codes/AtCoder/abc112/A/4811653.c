#include <stdio.h>

int main(void)
{
  	int N, A, B;
  	scanf("%d", &N);
  	if (N == 2) {
   		scanf("%d %d", &A, &B);
  		printf("%d", A + B);
    }
  	else
      	puts("Hello World");
  	return 0;
} 